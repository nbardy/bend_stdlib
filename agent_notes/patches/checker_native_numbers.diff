diff --git a/bend2/bend.ts b/bend2/bend.ts
index e3201d4..2e21aa5 100644
--- a/bend2/bend.ts
+++ b/bend2/bend.ts
@@ -2902,6 +2902,35 @@ export function body_flatten(b: Body, vars: PVar[], fr: () => number): LTerm {
 // a rewrite demands its evidence and steps to its body on {==}, else
 // sticks as a value.
 
+// Base Nat operations on closed numbers are computed natively, as the
+// compiler does, instead of unfolding Succ once per unit.
+const NAT: Record<Name, (a: number, b: number) => number | string> = {
+  "Nat.add": (a, b) => a + b, "Nat.sub": (a, b) => Math.max(a - b, 0), "Nat.mul": (a, b) => a * b,
+  "Nat.min": (a, b) => Math.min(a, b), "Nat.max": (a, b) => Math.max(a, b),
+  "Nat.cmp": (a, b) => a < b ? "LT" : a === b ? "EQ" : "GT",
+};
+const CLOSED = new WeakMap<HTerm, boolean>();
+function closed(t: HTerm): boolean {
+  let r = CLOSED.get(t);
+  if (r === undefined) {
+    r = t.$ === "Ref" || t.$ === "Lit" || (t.$ === "Var" && t.v !== undefined && closed(t.v))
+      || (t.$ === "App" && closed(t.f) && closed(t.x)) || (t.$ === "Ctr" && t.x.every(closed));
+    CLOSED.set(t, r);
+  }
+  return r;
+}
+function nat_of(book: Book, t: HTerm): number | null {
+  let k = 0;
+  for (t = term_wnf(book, t); t.$ === "Ctr" && t.k === "Succ"; t = term_wnf(book, t.x[0])) k += 1;
+  return t.$ === "Lit" && typeof t.v === "number" ? t.v + k : t.$ === "Ctr" && t.k === "Zero" ? k : null;
+}
+function nat_fast(book: Book, f: (a: number, b: number) => number | string, frs: Frame[], s?: Span): HTerm | null {
+  const [x, y] = [frs[frs.length - 1], frs[frs.length - 2]] as Extract<Frame, { $: "APP" }>[];
+  const a = closed(x.x) && closed(y.x) ? nat_of(book, x.x) : null;
+  const r = a === null ? null : ((b) => b === null ? null : f(a, b))(nat_of(book, y.x));
+  return typeof r === "string" ? Ctr(r, [], s) : r !== null && Number.isSafeInteger(r) ? Lit(r, s) as HTerm : null;
+}
+
 export function term_wnf(book: Book, term: HTerm): HTerm {
   const frs: Frame[] = [];
   let tm: HTerm = term;
@@ -2999,6 +3028,8 @@ export function term_wnf(book: Book, term: HTerm): HTerm {
         if (run < tld.n || tld.v === null) {
           break focus;
         }
+        const r = tld.b && NAT[tm.k] ? nat_fast(book, NAT[tm.k], frs, tm.s) : null;
+        if (r !== null) { frs.length -= 2; tm = r; continue main; }
         const rf: HTerm = tm;
         lhs = { t: () => rf, n: tld.n };
         tm = tld.v;
