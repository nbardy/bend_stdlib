diff --git a/bend2/bend.ts b/bend2/bend.ts
index e3201d4..ab71110 100644
--- a/bend2/bend.ts
+++ b/bend2/bend.ts
@@ -3276,7 +3276,8 @@ export function term_compare(mode: "EQ" | "LE", book: Book, lhs: HTerm, rhs: HTe
     }
     case "Ctr": {
       return b.$ === "Ctr" && a.k === b.k && a.x.length === b.x.length
-          && a.x.every((x, j) => term_compare("EQ", book, x, b.x[j], dep));
+          && a.x.every((x, j) => j === a.x.length - 1 || term_compare("EQ", book, x, b.x[j], dep))
+          && (a.x.length === 0 || term_compare("EQ", book, a.x[a.x.length - 1], b.x[b.x.length - 1], dep));
     }
     case "Lit": {
       return b.$ === "Lit" && a.v === b.v;
diff --git a/bend2/main.ts b/bend2/main.ts
index bfd3fa8..6c47590 100755
--- a/bend2/main.ts
+++ b/bend2/main.ts
@@ -636,7 +636,11 @@ function book_err(e: unknown): string {
     return "Error: the machine stack overflowed (a deep recursion, or a"
       + " literal too large to expand)";
   }
-  return err?.$ === "Err" ? Bend.err_show(err) : String(e);
+  try {
+    return err?.$ === "Err" ? Bend.err_show(err) : String(e);
+  } catch (x) {
+    return book_err(x);
+  }
 }
 
 // Load
diff --git a/bendc/tests/check/nat_long_chain.bend b/tests/check/nat_long_chain.bend
new file mode 100644
index 0000000..f83f701
--- /dev/null
+++ b/tests/check/nat_long_chain.bend
@@ -0,0 +1,11 @@
+# a 200000-deep Succ chain compares with its literal in constant stack:
+# a constructor's last field is compared by a tail call
+import Base
+
+law big:
+  {Nat.add(100000n, 100000n) == 200000n : Nat}
+
+def big():
+  {==}
+
+#|All terms check.
